import { PlayableExtractorPlugin, ResolveOptions, Song } from 'distube';

declare class DirectLinkPlugin extends PlayableExtractorPlugin {
    validate(url: string): Promise<boolean>;
    resolve<T>(url: string, options?: ResolveOptions<T>): Song<T>;
    getStreamURL(song: Song): string;
    getRelatedSongs(): never[];
}

export { DirectLinkPlugin };
